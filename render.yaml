services:
  - type: web
    name: ai-chatbot
    env: docker
    region: oregon
    plan: free
    repo: "REPLACE_WITH_GITHUB_REPO_URL"
    branch: main
    dockerfilePath: Dockerfile
    buildCommand: ""
    startCommand: ""
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: SERVICE_TOKEN
        sync: false
